/** Annotation holding YAML list of gRPC environments (name + target + security). */
export const GRPC_DOCS_ENVIRONMENTS_ANNOTATION = 'grpc-docs.io/environments';

/** Annotation holding YAML list of per-RPC playground options (examples, headers). */
export const GRPC_DOCS_OPERATIONS_ANNOTATION = 'grpc-docs.io/operations';
